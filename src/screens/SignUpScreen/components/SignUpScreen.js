import React from 'react';
import { View, Text } from 'react-native';

const SignUpScreen = () => (
    <View style={{flex: 1}}>
      <Text>
        Test complete
      </Text>
    </View>
  );

export default SignUpScreen;
