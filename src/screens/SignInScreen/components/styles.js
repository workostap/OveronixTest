import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  forgotPassword: {
    textAlign: 'right',
    width: '100%',
    marginBottom: 32,
  },
  signUpText: {
    marginTop: 40,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
